[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/sdp_control"]

[project]
name = "ska-sdp-long-observation"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "prefect>=3.8.6",
    "pytest>=9.1.1",
    "types-pyyaml>=6.0.12.20260906",
]

[dependency-groups]
dev = [
    "black>=26.5.1",
    "isort>=9.0.1",
]
